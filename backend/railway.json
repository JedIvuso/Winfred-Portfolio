{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && mkdir -p dist && npm run build"
  },
  "deploy": {
    "startCommand": "npm run start:prod",
    "healthcheckPath": "/",
    "healthcheckTimeout": 100
  }
}
