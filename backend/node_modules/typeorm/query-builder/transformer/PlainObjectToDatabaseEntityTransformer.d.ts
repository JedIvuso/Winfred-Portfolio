import { EntityMetadata } from "../../metadata-builder/metadata/EntityMetadata";
import { QueryBuilder } from "../QueryBuilder";
/**
 * Transforms plain old javascript object
 * Entity is constructed based on its entity metadata.
 */
export declare class PlainObjectToDatabaseEntityTransformer<Entity> {
    transform(object: any, metadata: EntityMetadata, queryBuilder: QueryBuilder<Entity>): Promise<Entity>;
    private join(qb, needToLoad, parentAlias);
    private buildLoadMap(object, metadata, isFirstLevelDepth?);
}
