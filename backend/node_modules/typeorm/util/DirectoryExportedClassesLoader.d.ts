/**
 * Loads all exported classes from the given directory.
 */
export declare class DirectoryExportedClassesLoader {
    /**
     * Imports all entities (makes them "require") from the given directories.
     */
    importClassesFromDirectories(directories: string[]): Function[];
    private loadFileClasses(exported, allLoaded);
}
export declare function importClassesFromDirectories(directories: string[]): Function[];
